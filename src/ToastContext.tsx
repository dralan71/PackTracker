// Toast context for custom hooks or future expansion (optional)
// Currently not used, but can be used for more advanced notification logic
import { createContext } from "react";

export const ToastContext = createContext({});
